apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  app: frontend-svc
spec:
  selector:
     {{- include "labels" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP